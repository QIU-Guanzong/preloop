"""A2A shaped records Preloop uses internally.

Nothing here serves or consumes A2A over the wire. The package holds the
object shapes internal features borrow from A2A so that a later serving or
governing surface inherits a data model instead of migrating one.
"""
